# The URL the site will be built for
base_url = "https://mkmk.me"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "hook"
taxonomies = [{ name = "categories" }, { name = "tags" }]
title = "@mkmkme"
description = "mkmkme's personal blog"


[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "agola-dark"

# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

# Whether smart punctuation is enabled (changing quotes, dashes, dots in their typographic form)
# For example, `...` into `…`, `"quote"` into `“curly”` etc
smart_punctuation = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false


[extra]
# Put all your custom variables here

links = [
    { title = "Mastodon", href = "https://mastodon.social/@mkmkme" },
    { title = "GitHub", href = "https://github.com/mkmkme" },
    { title = "LinkedIn", href = "https://linkedin.com/in/mkmkme" },
    { title = "CV", href = "files/cv.pdf" },
]

[extra.mastodon]
host = "mastodon.social"
username = "mkmkme"
